// Test file for enum declarations

enum Color {
    Red,
    Green,
    Blue
};

enum class Direction {
    North,
    South,
    East,
    West
};

enum class Status : char {
    Pending = 0,
    Active = 1,
    Done = 2
};

// #include <vector>

// enum A { A_A, A_B, A_C, A_D };
// enum class B { A, B, C, D };

// enum C : char { C_A, C_B, C_C, C_D };
// enum class D : char { A, B, C, D };

// namespace {
//   enum E { A_A, A_B, A_C, A_D };
//   enum class F { A, B, C, D };

//   enum G : char { C_A, C_B, C_C, C_D };
//   enum class H : char { A, B, C, D };
// }

// namespace detail {
//   enum A { A_A, A_B, A_C, A_D };
//   enum class B { A, B, C, D };

//   enum C : char { C_A, C_B, C_C, C_D };
//   enum class D : char { A, B, C, D };

//   namespace {
//     enum E { A_A, A_B, A_C, A_D };
//     enum class F { A, B, C, D };

//     enum G : char { C_A, C_B, C_C, C_D };
//     enum class H : char { A, B, C, D };
//   }
// }
